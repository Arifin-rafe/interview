// a= 5
// console.log(a) // 5
// var a

// console.log(0.1+0.2===0.3) //false
// console.log(0015) 

// what can we use to make site responsiveness?
// media queries

// what is a media query?
// media queries are used to apply different styles to different screen sizes

// what is z-index?
// z-index is used to specify the stack order of elements
// example: z-index: 1;

// what iss default position if position is not specified?
// default position is static

// what is the difference between call stack and event loop?
// call stack is a stack of functions that are being executed
// event loop is a loop that is used to process events

// what is closure?
// closure is a function that has access to variables from its outer function even after
// the outer function has finished executing. USED TO CREATE PRIVATE VARIABLES

// how to handle async and await error hadnling?
// use try catch block

// what is dependency in useEffect and how it works?
// dependency is a list of variables that useEffect depends on
// useEffect will only run if the dependency changes

// what will happen if useEffect dependency is set to null?

// what is context api?
// context api is a way to share data globally
// example: theme, user authentication

// explain redux components?
// redux components are used to manage state globally
// example: store, provider

// What is heigher order component in react?
// A Higher-Order Component (HOC) in React is a function that takes a component
// and returns a new component with enhanced functionality.
// It is a pattern for reusing component logic without repeating code.
// HOCs do not modify the original component; they create a wrapper component.
// Common use cases for HOCs include code reuse, state abstraction, and props manipulation.
// Example of a simple HOC that adds a loading spinner to a component

// useMemo vs useCallback?
// useMemo memoizes the result of a function (a value) to avoid expensive recalculations. example: computed values
// useCallback memoizes the function itself to prevent unnecessary re-creations on re-renders. example: event handlers



