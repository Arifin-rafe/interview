// if ([] == true) {
//   console.log("rafe here");
// } else {
//   console.log("rafe not here");
// } // type coience happens here output rafe not here


// let obj = {
//     a:{
//         b: undefined
//     }
// }
// console.log(obj.a?.b?.c?.d??"rafe") // null coience


// CSS border vs ouline?
// border is part of the box model and affects layout; it can be styled per side and supports border-radius.
// outline is drawn outside the element, doesn’t affect layout, and cannot be styled per side or rounded.
// Use border for visual design, and outline mainly for accessibility or focus indication.


// Difference between premitive vs non-premitive?
// PRIMITIVE DATA TYPES-`Number`, `String`, `Boolean`, `null`, `undefined`, `Symbol`, and `BigInt`. 
// They are immutable and stored by value
// NON-PRIMITIVE DATA TYPES
// Also called reference types include `Objects`, `Arrays`, `Functions`, and others. 
// These are mutable and stored by reference, so when you assign an object or array to a new variable,
// it points to the same memory location— changes to one affect the other. 

