// const arr=[8,1,5,4,6,11]
// const ans =[]
// for(let i = 0; i < arr.length-1; i++){
//     for(let j = i+1; j < arr.length; j++){
//         if(arr[i]+arr[j]==10){
//             ans.push(arr[i],arr[j])
//         }  
//     }
// }
// console.log(ans)

// console.log(isNaN("rafe")) //true

// console.log(isNaN("5")) //false

// let str = 'rafe';
// let str2 = 'developer';
// let result = '';
// let maxLen = Math.max(str.length, str2.length);
// for (let i = 0; i < maxLen; i++) {
//   if (i < str.length) result += str[i];
//   if (i < str2.length) result += str2[i];
// }
// console.log(result);

// In JavaScript, primitive data types
// (like string, number, boolean, null, undefined, symbol, bigint) are stored by value and are immutable.
// Non-primitive data types (like objects, arrays, functions) are stored by reference and are mutable,
// meaning changes affect all references.


// console.log({} === {}); // false, different references
// console.log([] === []); // false, different references